{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "RouterChangedMessage",
  "description": "Notification only, not event-carried state transfer: a router was created, updated or deleted - a consumer should call Devices.Service's API for current data, never reconstruct state from this event alone. No consumer exists yet - published as a low-cost hedge for future integrations.",
  "type": "object",
  "required": ["tenantId", "routerId", "action", "changedAt"],
  "properties": {
    "tenantId": { "type": "string", "format": "uuid" },
    "routerId": { "type": "string", "format": "uuid" },
    "action": { "type": "string", "enum": ["Created", "Updated", "Deleted"] },
    "changedAt": { "type": "string", "format": "date-time" }
  },
  "additionalProperties": false
}
