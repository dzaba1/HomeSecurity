{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "UpdateRouter",
  "description": "A full replacement of a router's metadata. Omit secret to keep the stored one; supply it to rotate. authMode is required for WebScrape and must be omitted for SNMP.",
  "type": "object",
  "required": ["name", "host", "protocol", "username"],
  "properties": {
    "name": { "type": "string", "minLength": 1, "maxLength": 200 },
    "host": { "type": "string", "minLength": 1, "maxLength": 255, "description": "Host name or IP address on the home network" },
    "protocol": { "$ref": "router_protocol.json" },
    "authMode": { "oneOf": [{ "$ref": "router_auth_mode.json" }, { "type": "null" }] },
    "username": { "type": "string", "maxLength": 200 },
    "secret": { "type": "string", "minLength": 1, "maxLength": 1024, "description": "Only when rotating the secret. Write-only: never returned by any endpoint." }
  },
  "additionalProperties": false
}
