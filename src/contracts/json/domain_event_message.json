{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "DomainEventMessage",
  "description": "One thing a service did to a tenant's data, access or credentials (a membership was added, a role was assigned, a router credential was fetched), published by the service that did it under a routing key equal to action. Any number of services may subscribe; Audit.Service is one of them and stores each event verbatim as the record of who did what, to which tenant's data, and when. Not event-carried state transfer: a subscriber that needs current state calls the owning service's API. metadata carries the action-specific extended data and must never carry a secret, a JWT, a plaintext credential or a raw log payload. See docs/architecture/16-auditing-and-compliance.md.",
  "type": "object",
  "required": ["eventId", "occurredAt", "tenantId", "actor", "action", "target", "metadata"],
  "properties": {
    "eventId": {
      "type": "string",
      "format": "uuid",
      "description": "Idempotency key: a subscriber drops a second event with the same id (Audit.Service does so by a unique index)."
    },
    "occurredAt": { "type": "string", "format": "date-time", "description": "Set by the publishing service when the action happened." },
    "tenantId": { "type": ["string", "null"], "format": "uuid", "description": "Null for pre-tenant events such as a login." },
    "actor": {
      "type": "object",
      "required": ["type", "id"],
      "properties": {
        "type": { "type": "string", "enum": ["User", "Device", "System"] },
        "id": { "type": "string", "minLength": 1, "maxLength": 200, "description": "Keycloak subject for a User, the agent credential id for a Device." }
      },
      "additionalProperties": false
    },
    "action": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_]*(\\.[a-z][a-z0-9_]*)+$",
      "maxLength": 100,
      "description": "Dotted lower-case event name, e.g. membership.added or router.credential.fetched. It is also the routing key the event is published under."
    },
    "target": {
      "type": "object",
      "required": ["type", "id"],
      "properties": {
        "type": { "type": "string", "minLength": 1, "maxLength": 100 },
        "id": { "type": "string", "minLength": 1, "maxLength": 200 }
      },
      "additionalProperties": false
    },
    "metadata": {
      "type": "object",
      "description": "Action-specific extended data (identifiers and the before/after of what changed); never a secret value.",
      "additionalProperties": true
    }
  },
  "additionalProperties": false
}
